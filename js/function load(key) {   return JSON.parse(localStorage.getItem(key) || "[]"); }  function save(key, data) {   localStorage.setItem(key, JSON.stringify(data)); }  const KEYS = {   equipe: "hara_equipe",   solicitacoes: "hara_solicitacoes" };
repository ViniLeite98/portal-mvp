function load(key) {
  return JSON.parse(localStorage.getItem(key) || "[]");
}

function save(key, data) {
  localStorage.setItem(key, JSON.stringify(data));
}

const KEYS = {
  equipe: "hara_equipe",
  solicitacoes: "hara_solicitacoes"
};
